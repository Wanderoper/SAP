TABLES: SSCRFIELDS. " 1000번 화면에 버튼달기 위해 필수임

DATA: OK_CODE TYPE SY-UCOMM.

*1) Excel 업로드 할 인터널 테이블 = EXCEL에 받을 값과 포맷을 맞추면 된다
DATA: BEGIN OF GW_DATA.
  DATA NAME(32).
  DATA TITLE(1).
  DATA TELEPHONE(13).
  DATA ADDRESS(32).
  DATA AGE TYPE I.
DATA: END OF GW_DATA.

" EXCEL->ITAB으로 받아오기 위함
DATA: GT_DATA LIKE TABLE OF GW_DATA.

" CALL METHOD로 받아올 파일경로+파일명
DATA: GT_FILE TYPE FILETABLE,
      GW_FILE LIKE LINE OF GT_FILE,
      GV_RC   TYPE I,
      GV_COL  TYPE I.

" 받아온 파일명으로 가져올 실제 DATA -> 추후 GT_DATA 에 넣어주기
DATA: GT_INTERN TYPE TABLE OF ALSMEX_TABLINE,
      GS_INTERN LIKE LINE OF GT_INTERN.

" EXCEL DOWNLOAD
DATA: GV_DOWNPATH TYPE RLGRAP-FILENAME,
      GV_FILENAME TYPE STRING,
      GV_PATH     TYPE STRING,
      GV_FULLPATH TYPE STRING.

DATA: GS_MIME     TYPE WWWDATATAB.

PARAMETERS P_OBJ  TYPE WWWDATA-OBJID.

FIELD-SYMBOLS: <FS_COMP> TYPE ANY.

* 사용자로부터 업로드할 엑셀 위치 입력 받기.
PARAMETERS: P_FILE TYPE RLGRAP-FILENAME. " 파일타입

PARAMETERS: P_FIXED TYPE C DEFAULT 'X'. " SPLITTER PARAM